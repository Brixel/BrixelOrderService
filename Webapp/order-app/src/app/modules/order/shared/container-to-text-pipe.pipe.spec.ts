import { ContainerToTextPipePipe } from './container-to-text-pipe.pipe';

describe('ContainerToTextPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ContainerToTextPipePipe();
    expect(pipe).toBeTruthy();
  });
});
